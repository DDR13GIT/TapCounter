<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/countingFragment">
    <fragment
        android:id="@+id/countingFragment"
        android:name="com.ddroy.tapcounter.ui.CountingFragment"
        android:label="fragment_counting"
        tools:layout="@layout/fragment_counting" >
        <action
            android:id="@+id/action_countingFragment_to_settingsFragment"
            app:destination="@id/settingsFragment" />
    </fragment>
    <fragment
        android:id="@+id/settingsFragment"
        android:name="com.ddroy.tapcounter.ui.SettingsFragment"
        android:label="fragment_settings"
        tools:layout="@layout/fragment_settings" />
</navigation>